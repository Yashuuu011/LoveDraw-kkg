# API URL for frontend production build
# Leave empty for Vercel unified deployment (uses relative /api)
# Set to your Railway server domain if frontend is hosted separately on Vercel:
# VITE_API_URL=https://your-railway-app.up.railway.app/api
VITE_API_URL=/api
